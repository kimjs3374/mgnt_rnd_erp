import { 단계판정, 미선정인가, type 과제단계 } from "@/lib/project-stage"

/**
 * 지원사업(비 R&D)도 과제사업과 같은 세 단계로 나눈다 — **신청중 → 수행중 → 사업종료**
 * (2026-09-04 사용자 지시: "과제와 같이 신청중 수행중 사업종료로 구분할 수 있게").
 *
 * 판정 로직은 새로 만들지 않는다 — `상태`·`선정결과`·`종료일`로 판정하는 규칙이
 * 과제사업과 완전히 같다(둘 다 `app.projects` 한 테이블이고, 지원사업은 그중
 * `사업유형`이 국가 R&D가 아닌 행일 뿐이다). `lib/project-stage.ts`의 `단계판정`·
 * `미선정인가`를 그대로 가져다 쓰고, 여기서는 **지원사업 쪽 경로**만 따로 정의한다.
 */
export type 사업단계 = 과제단계
export { 단계판정, 미선정인가 }

export const 단계정의: { 단계: 사업단계; 경로: string; 설명: string }[] = [
  {
    단계: "신청중",
    경로: "/programs/applying",
    설명: "지원을 넣고 결과를 기다리는 건. 선정을 기록하면 수행중으로 넘어간다.",
  },
  {
    단계: "수행중",
    경로: "/programs/executing",
    설명: "선정되어 수행기간 안에 있는 사업. 여기서 집행하고 증빙을 챙긴다.",
  },
  {
    단계: "사업종료",
    경로: "/programs/closed",
    설명: "수행기간이 끝난 사업. 정산·결과보고가 남아 있을 수 있다.",
  },
]

/**
 * ⚠ 「전체」 경로는 `/programs` 그대로 둔다 — 지원사업 대장(현재 「지원사업 관리」)이
 *   이미 이 주소였고, 여러 화면·링크·북마크가 가리키고 있다. 과제사업은 `/projects`가
 *   수행중에 이미 쓰이고 있어서 전체를 `/projects/all`로 새로 뺐지만, 지원사업 쪽은
 *   반대로 전체가 원래 자리를 갖고 있으니 **수행중을 `/programs/executing`으로 새로 뺀다.**
 */
export const 전체보기 = {
  경로: "/programs",
  설명: "신청 · 수행 · 종료를 한 표에서 본다. 상태 · 검색으로 걸러 낸다.",
}

/** 칩·메뉴에 쓸 순서. 전체가 맨 앞이다 — 넓은 데서 좁혀 들어가는 것이 읽는 순서다. */
export const 보기목록: { 이름: 사업단계 | "전체"; 경로: string }[] = [
  { 이름: "전체", 경로: 전체보기.경로 },
  ...단계정의.map((d) => ({ 이름: d.단계 as 사업단계 | "전체", 경로: d.경로 })),
]
